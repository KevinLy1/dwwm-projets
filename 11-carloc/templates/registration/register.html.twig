{% extends 'base.html.twig' %}

{% block body %}
    {% if not editMode %}
        <h1>Créer un compte</h1>
    {% else %}
        <h1>Édition de l'utilisateur</h1>
    {% endif %}

    {{ form_errors(registrationForm) }}

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.username, { 'label': 'Nom d\'utilisateur' }) }}
        {{ form_row(registrationForm.email, { 'label': 'Adresse e-mail' }) }}
        {% if not editMode %} {{ form_row(registrationForm.password) }} {% endif %}
        {{ form_row(registrationForm.firstName, { 'label': 'Prénom' }) }}
        {{ form_row(registrationForm.name, { 'label': 'Nom de famille' }) }}
        {{ form_row(registrationForm.sex, { 'label': 'Civilité' }) }}

        <button type="submit" class="btn btn-primary mt-3">💾 Valider</button>
    {{ form_end(registrationForm) }}
{% endblock %}
