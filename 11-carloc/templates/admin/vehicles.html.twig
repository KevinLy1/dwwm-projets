{% extends 'base.html.twig' %}

{% block body %}
    <div class="text-center my-5">
        <h1 class="my-4">Gestion des véhicules</h1>
        <div class="alert alert-info">💬 Vous trouverez ci-dessous le tableau contenant l'ensemble des véhicules</div>
        <div class="d-flex justify-content-center gap-4 mt-5">
            <a href="{{ path("admin") }}" class="btn btn-primary">Retour au PCA</a>
            <a href="{{ path("admin_vehicles_add") }}" class="btn btn-primary">Créer un véhicule</a>
        </div>
    </div>

    <table class="table table-bordered table-striped table-hover text-center">
        <thead class="table-dark">
            <th>Véhicule</th>
            <th>Titre</th>
            <th>Marque</th>
            <th>Modèle</th>
            <th>Description</th>
            <th>Photo</th>
            <th>Prix</th>
            <th>Date de création</th>
            <th>Date de mise à jour</th>
            <th>Voir</th>
            <th>Éditer</th>
            <th>Supprimer</th>
        </thead>

        <tbody>
            {% for vehicle in vehicles %}
                <tr>
                    <td>{{ vehicle.id }}</td>
                    <td>{{ vehicle.title }}</td>
                    <td>{{ vehicle.brand }}</td>
                    <td>{{ vehicle.model }}</td>
                    <td>{{ vehicle.description | raw }}</td>
                    <td><img src="{{ asset("assets/img/vehicles/" ~ vehicle.photo) }}" alt="Image du véhicule" width="200" height="150"></td>
                    <td>{{ vehicle.dailyPrice }}€</td>
                    <td>{{ vehicle.createdAt | date('d/m/Y à H:i:s') }}</td>
                    <td>{{ vehicle.updatedAt | date('d/m/Y à H:i:s') }}</td>
                    <td><a href="{{ path("show_vehicle", {"id": vehicle.id }) }}" class="btn btn-info"><i class="fa-solid fa-eye"></i></a></td>
                    <td><a href="{{ path("admin_vehicles_edit", {"id": vehicle.id }) }}" class="btn btn-warning"><i class="fa-solid fa-pen-to-square"></i></a></td>
                    <td><a href="{{ path("admin_vehicles_delete", {"id": vehicle.id }) }}" class="btn btn-danger"><i class="fa-solid fa-trash-can"></i></a></td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
